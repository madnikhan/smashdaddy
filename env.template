# =============================================================================
# SmashD / STACKD – Environment variables (copy to .env.local for local dev)
# For Vercel: set these in Project Settings → Environment Variables
# =============================================================================

# --- Required for production (Vercel) ---
NODE_ENV=production

# PostgreSQL (required on Vercel – use Neon, Supabase, or Vercel Postgres)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Add ?connection_limit=1 for serverless if needed
DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# NextAuth (required if using Google sign-in)
NEXTAUTH_URL="https://your-domain.vercel.app"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Google OAuth (required for NextAuth Google provider)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Public app URL (used in links and redirects)
NEXT_PUBLIC_APP_URL="https://your-domain.vercel.app"
NEXT_PUBLIC_BASE_URL="https://your-domain.vercel.app"

# --- Optional: real-time / notifications ---
# Upstash Redis (optional – order broadcast; app works without it)
# UPSTASH_REDIS_REST_URL=""
# UPSTASH_REDIS_REST_TOKEN=""

# --- Optional: payments ---
# SumUp (till / in-person)
SUMUP_API_KEY=""
SUMUP_MERCHANT_ID=""
SUMUP_MERCHANT_CODE=""
SUMUP_ACCESS_TOKEN=""
SUMUP_ENVIRONMENT="sandbox"

# Stripe (online payments)
# STRIPE_PUBLISHABLE_KEY=""
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""

# --- Optional: SEO / verification ---
# GOOGLE_SITE_VERIFICATION=""

# --- Optional: debugging ---
# PRISMA_LOG_QUERIES="true"
